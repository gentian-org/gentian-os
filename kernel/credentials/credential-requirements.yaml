# GENERATED FILE — DO NOT EDIT.
#
# Rendered by scripts/gen-credential-requirements.py from credentials.yaml.
# To change a requirement, edit credentials.yaml and run `make gen-all`.
#
# These are the catalogue's on-cluster carrier: the credential manager reads
# CredentialRequirement objects from the API, while the installer reads
# credentials.yaml from disk before any cluster exists. Same content, two
# carriers, one source.

apiVersion: gentianos.io/v1alpha1
kind: CredentialRequirement
metadata:
  name: deployments-repository
  labels:
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-scope: cluster
spec:
  displayName: Deployments Repository Access
  phase: bootstrap
  scope: cluster
  vaultPath: gentian/repositories/deployments
  fields:
  - key: username
    format: string
    secret: false
    example: x-access-token
  - key: token
    format: token
    secret: true
  description: 'Token for the Git repository holding this cluster''s configuration. The installer needs
    it before it can clone the repository that would otherwise describe it, so it is supplied at the prompt
    rather than declared.

    '
  optional: false
  validate:
    type: git-https
  consumedBy:
  - kind: XRepository
    name: deployments
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credreq-deployments-repository
  namespace: gentian-system
  labels:
    gentianos.io/credential-requirement: deployments-repository
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-optional: 'false'
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    creationPolicy: None
  data:
  - secretKey: username
    remoteRef:
      key: gentian/repositories/deployments
      property: username
  - secretKey: token
    remoteRef:
      key: gentian/repositories/deployments
      property: token
---
apiVersion: gentianos.io/v1alpha1
kind: CredentialRequirement
metadata:
  name: master-password
  labels:
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-scope: cluster
spec:
  displayName: Master Password
  phase: bootstrap
  scope: cluster
  vaultPath: gentian-os/kernel/internal/master-password
  fields:
  - key: password
    format: password
    secret: true
    minLength: 16
  - key: salt
    format: string
    secret: true
  description: 'Root of every derived credential in the cluster. Database users, service accounts and
    app passwords are HMAC-SHA256 of this value plus a salt, so it is the single most important secret
    an operator supplies and the only one whose loss cannot be repaired by rotation alone.

    '
  optional: false
  validate:
    type: noop
  consumedBy:
  - kind: XCluster
    name: cluster
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credreq-master-password
  namespace: gentian-system
  labels:
    gentianos.io/credential-requirement: master-password
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-optional: 'false'
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    creationPolicy: None
  data:
  - secretKey: password
    remoteRef:
      key: gentian-os/kernel/internal/master-password
      property: password
  - secretKey: salt
    remoteRef:
      key: gentian-os/kernel/internal/master-password
      property: salt
---
apiVersion: gentianos.io/v1alpha1
kind: CredentialRequirement
metadata:
  name: infra-chart-registry
  labels:
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-scope: cluster
spec:
  displayName: Infrastructure Chart Registry
  phase: bootstrap
  scope: cluster
  vaultPath: gentian/registries/infra-charts
  fields:
  - key: username
    format: string
    secret: false
  - key: password
    format: password
    secret: true
  description: 'Credentials for the registry supplying infrastructure Helm charts and container images.
    A cluster pulling only from public registries can leave this unset.

    '
  optional: true
  validate:
    type: oci-registry
  consumedBy:
  - kind: XRepository
    name: infra-charts
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credreq-infra-chart-registry
  namespace: gentian-system
  labels:
    gentianos.io/credential-requirement: infra-chart-registry
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-optional: 'true'
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    creationPolicy: None
  data:
  - secretKey: username
    remoteRef:
      key: gentian/registries/infra-charts
      property: username
  - secretKey: password
    remoteRef:
      key: gentian/registries/infra-charts
      property: password
---
apiVersion: gentianos.io/v1alpha1
kind: CredentialRequirement
metadata:
  name: acme-dns-cloudflare
  labels:
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-scope: cluster
spec:
  displayName: Cloudflare DNS API Token
  phase: bootstrap
  scope: cluster
  vaultPath: gentian/dns/cloudflare
  fields:
  - key: api-token
    format: token
    secret: true
  description: 'Token with DNS edit rights on the kernel domain''s zone, used for DNS-01 ACME challenges.
    Required when the cluster issues a wildcard certificate; not needed for HTTP-01, a private CA, or
    self-signed issuance.

    '
  optional: true
  validate:
    type: oidc-discovery
    url: https://api.cloudflare.com/client/v4/user/tokens/verify
  consumedBy:
  - kind: XCluster
    name: cluster
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credreq-acme-dns-cloudflare
  namespace: gentian-system
  labels:
    gentianos.io/credential-requirement: acme-dns-cloudflare
    gentianos.io/credential-phase: bootstrap
    gentianos.io/credential-optional: 'true'
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    creationPolicy: None
  data:
  - secretKey: api-token
    remoteRef:
      key: gentian/dns/cloudflare
      property: api-token
---
apiVersion: gentianos.io/v1alpha1
kind: CredentialRequirement
metadata:
  name: smtp-relay
  labels:
    gentianos.io/credential-phase: runtime
    gentianos.io/credential-scope: cluster
spec:
  displayName: SMTP Relay Credentials
  phase: runtime
  scope: cluster
  vaultPath: gentian-os/kernel/mail/relay
  fields:
  - key: username
    format: string
    secret: false
    example: notifications@example.com
  - key: password
    format: password
    secret: true
  - key: host
    format: hostname
    secret: false
    example: smtp.example.com
  - key: port
    format: port
    secret: false
    example: '587'
  description: 'Credentials for the upstream mail relay, used when the cluster sends mail through an external
    provider rather than running its own Postfix.

    '
  optional: true
  validate:
    type: smtp
  consumedBy:
  - kind: XCluster
    name: cluster
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credreq-smtp-relay
  namespace: gentian-system
  labels:
    gentianos.io/credential-requirement: smtp-relay
    gentianos.io/credential-phase: runtime
    gentianos.io/credential-optional: 'true'
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    creationPolicy: None
  data:
  - secretKey: username
    remoteRef:
      key: gentian-os/kernel/mail/relay
      property: username
  - secretKey: password
    remoteRef:
      key: gentian-os/kernel/mail/relay
      property: password
  - secretKey: host
    remoteRef:
      key: gentian-os/kernel/mail/relay
      property: host
  - secretKey: port
    remoteRef:
      key: gentian-os/kernel/mail/relay
      property: port
---
apiVersion: gentianos.io/v1alpha1
kind: CredentialRequirement
metadata:
  name: argocd-github-webhook
  labels:
    gentianos.io/credential-phase: runtime
    gentianos.io/credential-scope: cluster
spec:
  displayName: ArgoCD GitHub Webhook Secret
  phase: runtime
  scope: cluster
  vaultPath: gentian/argocd/github-webhook
  fields:
  - key: secret
    format: token
    secret: true
  description: 'Shared secret validating inbound GitHub webhooks to ArgoCD. Without it ArgoCD still syncs
    on its polling interval, so this only shortens the delay between a push and a sync.

    '
  optional: true
  validate:
    type: noop
  consumedBy:
  - kind: XCluster
    name: cluster
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credreq-argocd-github-webhook
  namespace: gentian-system
  labels:
    gentianos.io/credential-requirement: argocd-github-webhook
    gentianos.io/credential-phase: runtime
    gentianos.io/credential-optional: 'true'
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    creationPolicy: None
  data:
  - secretKey: secret
    remoteRef:
      key: gentian/argocd/github-webhook
      property: secret
