# MinIO dev — secrets + env-specific sizes (app defaults in _base.yaml)
# auth.rootPassword removed — MinIO reads root credentials from the minio-credentials Secret
# provisioned by ExternalSecret (ESO → OpenBao → gentian-os/kernel/storage/minio).
auth:
  existingSecret: minio-credentials
  rootPasswordSecretKey: root-password
persistence:
  size: 10Gi
provisioning:
  enabled: false
resources:
  limits:
    cpu: "4"
    memory: 2Gi
  requests:
    cpu: 250m
    memory: 256Mi
statefulset:
  replicaCount: 1
