# Moved to InfraData prerequisites

MinIO ExternalSecrets are synced by `kernel/appsets/08-infra-data.yaml` from
`kernel/services/infra-minio/manifests/<env>/`.

Helm values layering reference: `../values/` (merged into infra-minio ConfigMaps).
