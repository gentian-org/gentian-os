apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: minio-provisioning-users
  namespace: gentian-infra-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: minio-provisioning-users
    creationPolicy: Owner
    # The MinIO provisioning job consumes usersExistingSecrets as files.
    # Each key is treated as a filename whose value is an ini-format block:
    #   username=<user>
    #   password=<pass>
    #   disabled=false
    #   policies=<policy>
    #   setPolicies=true
    template:
      type: Opaque
      data:
        migration_user: |
          username=migration_user
          password={{ .migrations_password }}
          disabled=false
          policies=migrations-bucket-policy
          setPolicies=true
        nextcloud_user: |
          username=nextcloud_user
          password={{ .nextcloud_password }}
          disabled=false
          policies=nextcloud-bucket-policy
          setPolicies=true
        notes_user: |
          username=notes_user
          password={{ .notes_password }}
          disabled=false
          policies=notes-bucket-policy
          setPolicies=true
        openproject_user: |
          username=openproject_user
          password={{ .openproject_password }}
          disabled=false
          policies=openproject-bucket-policy
          setPolicies=true
        openxchange_user: |
          username=openxchange_user
          password={{ .openxchange_password }}
          disabled=false
          policies=openxchange-bucket-policy
          setPolicies=true
        ums_user: |
          username=ums_user
          password={{ .ums_password }}
          disabled=false
          policies=ums-bucket-policy
          setPolicies=true
  data:
    - secretKey: migrations_password
      remoteRef:
        key: gentian-os/kernel/storage/minio
        property: migrations_password
    - secretKey: nextcloud_password
      remoteRef:
        key: gentian-os/kernel/storage/minio
        property: nextcloud_password
    - secretKey: notes_password
      remoteRef:
        key: gentian-os/kernel/storage/minio
        property: notes_password
    - secretKey: openproject_password
      remoteRef:
        key: gentian-os/kernel/storage/minio
        property: openproject_password
    - secretKey: openxchange_password
      remoteRef:
        key: gentian-os/kernel/storage/minio
        property: openxchange_password
    - secretKey: ums_password
      remoteRef:
        key: gentian-os/kernel/storage/minio
        property: ums_password
