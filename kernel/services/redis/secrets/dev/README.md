# Moved to InfraData prerequisites

Redis ExternalSecrets are synced by `kernel/appsets/08-infra-data.yaml` from
`kernel/services/infra-redis/manifests/<env>/`.

Helm values layering reference: `../values/` (merged into infra-redis ConfigMaps).
