# Standalone Keycloak (Stage 1) — plain Helm values for keycloakx chart.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-idp-base-values
  namespace: platform-kernel
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  values.yaml: |
    replicas: 1
    command:
      - "/opt/keycloak/bin/kc.sh"
    args:
      - "start"
      - "--http-enabled=true"
      - "--hostname=https://id.desk.gentian.org/auth"
      - "--hostname-backchannel-dynamic=true"
      - "--hostname-strict=false"
      - "--proxy-headers=xforwarded"
      - "--cache=local"
    proxy:
      mode: xforwarded
    service:
      type: ClusterIP
      httpPort: 8080
    ingress:
      enabled: false
    metrics:
      enabled: false
    database:
      vendor: postgres
      hostname: gentian-postgresql-dev.gentian-infra-dev.svc.cluster.local
      port: 5432
      database: keycloak
      username: keycloak_user
    extraEnv: |
      - name: KC_HEALTH_ENABLED
        value: "true"
      - name: KC_METRICS_ENABLED
        value: "false"
      - name: KC_CACHE
        value: "local"
      - name: KC_HOSTNAME
        value: "https://id.desk.gentian.org/auth"
      - name: KC_HOSTNAME_BACKCHANNEL_DYNAMIC
        value: "true"
      - name: KC_HOSTNAME_STRICT
        value: "false"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-idp-dev-values
  namespace: platform-kernel
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  values.yaml: |
    resources:
      requests:
        cpu: 100m
        memory: 512Mi
      limits:
        cpu: "2"
        memory: 1Gi
