# UNTESTED — provider-helm Release for public bokysan/mail chart.
# Chart repo: https://bokysan.github.io/docker-postfix/  Image: docker.io/boky/postfix
apiVersion: helm.crossplane.io/v1beta1
kind: Release
metadata:
  name: postfix-dev
  annotations:
    crossplane.io/external-name: postfix-dev
    argocd.argoproj.io/sync-wave: "9"
spec:
  forProvider:
    chart:
      name: mail
      repository: https://bokysan.github.io/docker-postfix/
      version: "5.1.0"
    namespace: gentian-dev
    wait: true
    waitTimeout: "5m"
    valuesFrom:
      - configMapKeyRef:
          name: postfix-base-values
          namespace: gentian-dev
          key: values.yaml
          optional: false
      - configMapKeyRef:
          name: postfix-dev-values
          namespace: gentian-dev
          key: values.yaml
          optional: false
      - secretKeyRef:
          name: postfix-sensitive-values
          namespace: gentian-dev
          key: sensitive-values.yaml
          optional: true
  providerConfigRef:
    name: kubernetes
