# =============================================================================
# OX App Suite — dev environment overrides
# =============================================================================
# NOTE: This file is NOT in the helm_release.values chain.
# Mail server hostnames are now set via sensitive.yaml.tftpl (templatefile).
# Keeping this file for reference only.
# =============================================================================

appsuite:
  # Dev: minimal resource allocation
  core:
    servers:
      nodes:
        groupware:
          replicas: 1
