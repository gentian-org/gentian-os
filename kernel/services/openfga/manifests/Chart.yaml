apiVersion: v2
name: openfga
description: |
  Per-stage manifests for openfga, rendered from a single source.
  The ApplicationSet passes env; adding a stage needs no new files.
type: application
version: 0.1.0
