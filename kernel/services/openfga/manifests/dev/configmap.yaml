# OpenFGA — plain Helm values (merged by Suze Crossplane composition).
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: openfga-base-values
  namespace: platform-kernel
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  values.yaml: |
    replicaCount: 1
    postgresql:
      enabled: false
    mysql:
      enabled: false
    datastore:
      engine: postgres
      maxOpenConns: 30
      maxIdleConns: 10
      connMaxIdleTime: 30s
      connMaxLifetime: 60s
      applyMigrations: true
      # initContainer avoids a separate migrate Job that Helm/provider-helm can
      # miss on failed installs (Deployment init then waits forever for the Job).
      migrationType: initContainer
      waitForMigrations: false
    grpc:
      enabled: true
    telemetry:
      trace:
        enabled: false
      metrics:
        enabled: false
    authn:
      method: preshared
    ingress:
      enabled: false
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: openfga-dev-values
  namespace: platform-kernel
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  values.yaml: |
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: "1"
        memory: 512Mi
