apiVersion: v2
name: dovecot
description: |
  Per-stage manifests for dovecot, rendered from a single source.
  The ApplicationSet passes env; adding a stage needs no new files.
type: application
version: 0.1.0
