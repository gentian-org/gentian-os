apiVersion: v2
name: openbao-config
description: |
  Per-stage manifests for openbao-config, rendered from a single source.
  The ApplicationSet passes env; adding a stage needs no new files.
type: application
version: 0.1.0
