# Valid minimal Cluster claim — only kernelDomain is required.
# Used by test-unit-schema to verify the XRD accepts this input.
apiVersion: gentianos.io/v1alpha1
kind: Cluster
metadata:
  name: test-minimal
  namespace: default
spec:
  kernelDomain: desk.gentian.org
  compositeDeletePolicy: Background
