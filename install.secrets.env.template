# Gentian OS installer secrets template
#
# Usage:
#   cp install.secrets.env.template install.secrets.env
#   edit install.secrets.env
#   chmod 600 install.secrets.env
#   ./install.sh
#
# IMPORTANT: Do not commit install.secrets.env.

MASTER_PASSWORD=replace-with-strong-master-password
# SMTP relay credentials — required only when MAIL_SERVICE_MODE=external in
# clusters/<cluster>/kernel/cluster-settings.env. Kernel mode uses in-cluster
# Postfix for Keycloak invitation/reset emails (no relay creds needed).
# SMTP_RELAY_USERNAME=replace-with-smtp-username
# SMTP_RELAY_PASSWORD=replace-with-smtp-password

# Optional: only needed for DNS-01 wildcard issuance via Cloudflare.
# Leave empty to skip the kernel wildcard (platform UIs). Tenant apps still need
# DNS-01 per-tenant wildcards via the operator (TENANT_DNS01_CLUSTER_ISSUER).
# Installer verifies the token against a Cloudflare zone derived from KERNEL_DOMAIN.
# Override the derived zone only when needed (e.g. compound public suffixes).
# CF_API_TOKEN=
# CF_ZONE_NAME=

# Optional: GitHub PAT for in-cluster app install/uninstall (App Store button and
# operator lifecycle API). install.sh creates Secret gentian-deployments-git-credentials
# in gentian-system and mounts it on the operator. Required for App Store installs
# from inside the cluster; CLI installs from your laptop use your local git credentials.
# Use a fine-scoped PAT with contents:write on gentian-deployments.
# GENTIAN_DEPLOYMENTS_GIT_TOKEN=
# GENTIAN_DEPLOYMENTS_GIT_USERNAME=x-access-token

# Optional: GitHub Actions automation (CI_BOT_PAT).
# gentian-ui builds images; reusable workflows in gentian-os commit new tags.
# Cross-repo workflow_call requires CI_BOT_PAT on both gentian-os and gentian-ui
# (same value; gentian-ui pass-through only). install.sh uploads via gh when logged in.
# Fine-grained PAT: gentian-org/gentian-os, Contents → Read and write.
# CI_BOT_PAT=
# Optional: immediate ArgoCD sync after pin (defaults ARGOCD_SERVER from KERNEL_DOMAIN).
# ARGOCD_SERVER=https://argocd.example.com
# ARGOCD_TOKEN=

# Optional: OpenProject Enterprise token for portal SSO (host must match
# projects.<tenant-domain>, e.g. projects.demo.desk.gentian.org).
# OPENPROJECT_ENTERPRISE_TOKEN=
