# Terraform / OpenTofu runtime artifacts
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Local installer credentials cache (install.sh)
.install-secrets.env
.install-state.env

# Local operator-provided install config (templates are committed as *.template)
install.env
install.secrets.env
